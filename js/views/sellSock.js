define(function(){
  return React.createClass({
    render: function(){
      return React.DOM.div(
        {className:"sell-sock"},
        React.DOM.h2(null,"Sell your Socks here!")
      )
    }

  })
})
